"""
You will need to modify these variables to suite your needs.
Also, i would convert these to encrypted values pulled from somewhere and/or use ssh keys if your network allows,
and exclude this directory from GIT
"""
from credlib import credential
MY_WR_DATABASE_CREDENTIALS = credential("", "", "", "")